# REQUIRED in production for CORS and WebSocket security
# Set to your frontend application's URL (e.g., https://app.example.com)
# Defaults to http://localhost:4200 in development only
TW_FRONTEND_URL=http://localhost:4200
# IMPORTANT: Generate a strong, random secret key for JWT tokens
# Use: openssl rand -base64 32
TW_SECRET_KEY=your-super-secret-jwt-key-min-32-chars-CHANGE-THIS
TW_REDIS_HOST=redis
TW_REDIS_PORT=6379
TW_ENABLED_INTEGRATIONS=
# Database configuration
# For external PostgreSQL, parse the connection string:
# postgres://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=prefer
# Example: postgres://pg-user:secret@db.example.com:5432/postgres?sslmode=prefer
TW_DB_NAME=postgres
TW_DB_HOST=your-db-host.example.com
TW_DB_PORT=5432
TW_DB_USERNAME=pg-user
TW_DB_PASSWORD=your-database-password
TW_DB_ENABLE_SSL=true
# Set to 'false' only for self-signed certificates (sslmode=prefer)
TW_DB_SSL_REJECT_UNAUTHORIZED=false
TW_VITE_API_URL=http://localhost:3000/v1